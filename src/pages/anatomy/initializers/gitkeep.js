import React from 'react';
import Link from 'gatsby-link';
import Template from '../../../components/templates/Anatomy';
import Gitkeep from '../../../components/Gitkeep';

export default (props) => {
  return (
    <Template>
      <h1>
        initializers/.gitkeep
      </h1>
      <Gitkeep/>
    </Template>
  );
};
